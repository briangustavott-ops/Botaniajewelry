# Setup 02 — N8N

## Requisitos previos

- Docker + Docker Compose instalados (ver [Setup 01](01-docker.md))
- VM accesible en `192.168.137.10`
- Repositorio clonado en `~/botania-repo` (ver [Setup 03](03-sqlite.md))

---

## 1. Crear la estructura de proyecto

```bash
mkdir -p ~/botania/data/n8n
cd ~/botania
```

---

## 2. Crear el archivo `.env`

```bash
nano ~/botania/.env
```

Contenido:

```env
# VM
VM_IP=192.168.137.10

# N8N
N8N_USER=admin
N8N_PASSWORD=cambia_esta_password
N8N_SECURE_COOKIE=false
```

---

## 3. Copiar los archivos de infra desde el repositorio

El `docker-compose.yml` y el `Dockerfile` están versionados en `infra/`:

```bash
cp ~/botania-repo/infra/docker-compose.yml ~/botania/
cp ~/botania-repo/infra/Dockerfile ~/botania/
```

### `docker-compose.yml`

```yaml
services:

  n8n:
    build:
      context: .
      dockerfile: Dockerfile
    image: botania/n8n:local
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5678:5678"
    environment:
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=${N8N_USER}
      - N8N_BASIC_AUTH_PASSWORD=${N8N_PASSWORD}
      - N8N_HOST=${VM_IP}
      - N8N_PORT=5678
      - N8N_PROTOCOL=http
      - WEBHOOK_URL=http://${VM_IP}:5678/
      - GENERIC_TIMEZONE=Europe/Madrid
      - TZ=Europe/Madrid
      - N8N_SECURE_COOKIE=${N8N_SECURE_COOKIE}
    volumes:
      - ./data/n8n:/home/node/.n8n
      - ./data/db:/data/db
```

### `Dockerfile`

Extiende la imagen oficial añadiendo herramientas de compilación necesarias para nodos de comunidad con módulos nativos (como `sqlite3`):

```dockerfile
FROM n8nio/n8n:latest

USER root
RUN apk add --no-cache python3 make g++
USER node
```

> **¿Por qué un Dockerfile propio?** El nodo de comunidad `n8n-nodes-sqlite` requiere compilar bindings nativos de Node.js. La imagen base de N8N (Alpine Linux) no incluye `python3`, `make` ni `g++` — sin ellos la instalación falla.

> **`N8N_SECURE_COOKIE=false`** es necesario porque el acceso es por HTTP local (sin TLS).

---

## 4. Construir la imagen y arrancar N8N

```bash
cd ~/botania

# Construir la imagen personalizada (solo la primera vez o tras cambios en el Dockerfile)
docker compose build

# Arrancar en background
docker compose up -d
```

Verificar que el contenedor está corriendo:

```bash
docker compose ps
docker compose logs n8n
```

---

## 5. Verificar acceso desde Windows

Abrir en el navegador de Windows:

```
http://192.168.137.10:5678
```

Debe aparecer la pantalla de login de N8N. Usar las credenciales del `.env`.

---

## 6. Instalar el nodo SQLite de comunidad

Una vez N8N arrancado, instalar el nodo de comunidad para SQLite:

1. Settings → **Community Nodes** → **Install**
2. Paquete: `n8n-nodes-sqlite`
3. Aceptar el aviso y confirmar

La compilación ahora funciona porque el `Dockerfile` incluye las herramientas necesarias.

---

## 7. Comandos útiles

```bash
# Ver logs en tiempo real
docker compose logs -f n8n

# Parar N8N
docker compose stop

# Arrancar N8N
docker compose start

# Reiniciar
docker compose restart n8n

# Actualizar N8N a nueva versión
# (editar FROM en Dockerfile si se quiere fijar versión)
docker compose build --no-cache && docker compose up -d
```

---

## Resultado

| Servicio | URL | Credenciales |
|---------|-----|--------------|
| N8N | `http://192.168.137.10:5678` | admin / [password del .env] |

Los datos de N8N (workflows, credenciales, nodos de comunidad) se guardan en `~/botania/data/n8n/` — basta con copiar esa carpeta para hacer backup.
